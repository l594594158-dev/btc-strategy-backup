# 2026-05-06 日记

## 重要修复

### 微信通知 Bug（严重）
- **问题**：`send_notify.py` 调用 `openclaw` 命令发送通知，但用的是相对路径，系统找不到，通知全部失败
- **修复**：改为绝对路径 `/root/.local/share/pnpm/openclaw`
- **验证**：测试成功，用户应已收到微信测试消息
- **版本**：v2.11.8

### OHLCV 数据 Bug
- **问题**：fetch_ohlcv 返回 2019 年旧数据（$10,000），导致指标计算全部错误
- **原因**：Binance期货 API 缓存问题，`since` 参数导致返回旧数据
- **修复**：加 `s` 参数强制拉最近1小时数据；加新鲜度校验重新获取
- **版本**：v2.11.6

### ATR 计算 Bug
- **问题**：`since` 参数导致数据不足（48条），ATR 计算失败
- **修复**：改为不传 since 参数，用 `limit` 和 `before` 参数限制
- **版本**：v2.11.7

### ADX 指标路径 Bug
- **问题**：`ta.volatility.ADXIndicator` 路径不存在，一直走异常分支，ADX 永远返回默认值 25
- **修复**：改为 `ta.trend.ADXIndicator`
- **版本**：v2.11.4

## 当前持仓
- **LONG 0.03 BTC @ $81,281**（Bot 信号触发自动开仓）
- 止损 $78,842 | 止盈 $85,345
- reason 已修正为 `bot_recovered`
- 移动止盈激活线 $81,000

## 用户偏好
- 开仓不通知用户 → 已修复微信通知
- RSI 阈值：震荡 70，做多 35-60，做空 82
- 不手动干预，只看信号自动执行

## 当前行情（01:50）
- 价格 $81,xxx | RSI 40+ | %b 0.2x | ADX 34
- 暂无信号，继续监控